

func add_with_sleep(n) {
    var idx = 0;
    var sum = 0;
    while (idx < n) {
        sum = sum + idx;
        sleep_sec(1);
        idx = idx + 1;
    }

    return sum;
}

var result = call_async(add_with_sleep, [10]);
println(result);