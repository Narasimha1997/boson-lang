
func fib(N) {
    
    if (N == 0) {
        return 0;
    }

    if (N == 1) {
        return 1;
    }

    return fib(N - 1) + fib(N - 2);
}

const st = unix_time();
const result = fib(10);
const et = unix_time();

println(result, et - st);