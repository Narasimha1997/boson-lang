const sys = mopen("std::syscalls", none)[0]

const arch_syscalls = sys.get_syscalls()

const string_to_write = "hello, world!"

sys.call(
    arch_syscalls.WRITE,
    1,
    string_to_write, 
    len(string_to_write)
)
