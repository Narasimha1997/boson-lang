

const N = 10000;

var arr = create_array(N, 0)
var idx = 0;

const st = unix_time();

while (idx < N) {
    arr[idx] = rand();
    idx = idx + 1;
}

sort(arr, true)

const et = unix_time();

println(arr)

println("time taken", et - st)