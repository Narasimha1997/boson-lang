
var adder = lambda a, b => a + b;

var times = 0;

const t1 = unix_time();
while (times != 5000) {
    var result = adder(10, 30);
    println(result);
    times = times + 1;
}
const t2 = unix_time();


println("Function Eval time", t2 - t1);
