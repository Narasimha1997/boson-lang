println("Hello World!!");

const radius = 10;

const area = 3.14*radius*radius;
const circumference = 2 * area / radius;

println("Area of circle is: ", area);

println(
    "Circumference of the circle is :", circumference
);