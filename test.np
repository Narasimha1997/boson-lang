println("Hello World!!");

const arr = [1,2,3,4, 5];
println(arr);

println(builtins());

var part_a = lambda x, m => x * m;
var y_intercept = lambda x, m, c => part_a(x, m) + c;

var result = y_intercept(10, 0.5, 1);
println(result);